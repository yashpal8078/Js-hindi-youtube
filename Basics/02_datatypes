"use Strict"; // treat all js code as new version

// alert("hello") we are using node js not browser 

console.log(3+3)
console.log("hitesh");

let name = "Yaashpal"
let age = 19
let ifLoogedIn = false
let state  = null

// number => 2 to power 53
// bigint

//string => doublecourts
// boolean
//null standalone value in js empty  also na object type
// undefined => variable is declare but ot assign 
// symbol => mostly used for unique nes

// objects 

console.log(typeof(age));
